1.
	It resolves to IP 1.2.3.4
	It is not reachable
	Error is "Request timed out"

2.
	Answer is coming from the DNS server

3.
	It dies at the first hop
	
4.
	`ping` command works, `nslookup` still cannot find the server. This is because the IP address 127.0.0.1 points at localhost, which is accessible, but the DNS still cannot solve for it

5.
	The outage was caused by a wrong IP address binding that could not be resolved by the DNS
	In a real world application, this misconfiguration could be caused by a vpn, or an antivirus application
	Prevention is done by correctly configuring the settings of any sych applications